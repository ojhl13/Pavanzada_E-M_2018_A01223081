#include <stdio.h>

void processSignal ( int Signal)
{
  printf("Got signal %d\n", signa );
  nofinish = =;

}

int int main(int argc, char const *argv[]) {
  /* code */

  signal( SIGUSR1,processSignal)
  while (nofinish)
  {
    
  }
  return 0;
}
